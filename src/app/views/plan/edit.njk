{% extends 'layout.njk' %}

{% block head %}
    <title>Edit plan | FIMS</title>
{% endblock %}

{% block content %}
    <div class="card">
        <section class="avatar" style="background: url({{schedule.avatar_url}}) no-repeat center center / cover; border-radius: 4px 0 0 4px;"></section>
        <section class="details">
            <form method="POST" action="/plan?_method=PUT">
                <h3>Edit plan</h3>
                {% include 'plan/fields.njk' %}
                <input type="hidden" name="id" value="{{plan.id}}">
            </form>
            <form id="form-delete" method="POST" action="/plan?_method=DELETE">
                <input type="hidden" name="id" value="{{plan.id}}">
                <button type="submit">Delete</button>
            </form>
        </section>
    </div>
{% include 'delete-confirmation.njk' %}
{% endblock content %}